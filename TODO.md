- Does bare-native or the Holepunch Android runtime expose any Android API bridge you're aware of?
- Alternatively, does the Bare build support a native addon (.node or .bare) where you could call
  WifiManager.acquireMulticastLock() via JNI/NDK?
